steps:
  # Install dependencies
  - name: node:16.13.0
    entrypoint: yarn
    args: ["install"]
  # Run custom commands
  - name: node
    entrypoint: yarn
    args: ["run", "build"]
  - name: "gcr.io/cloud-builders/gsutil"
    args: ["-m", "cp", "-r", "build/*", "gs://www.matlau.co.uk"]
