steps:
  # Install dependencies
  - name: node:16.13.0
    entrypoint: yarn
    args: ["install"]
  # Run custom commands
  - name: node:16.13.0
    entrypoint: yarn
    args: ["run", "build"]
  - name: "gcr.io/cloud-builders/gsutil"
    args: ["-m", "cp", "-r", "build/*", "gs://${_BUCKET_NAME}"]
